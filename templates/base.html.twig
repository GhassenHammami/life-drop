<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                LifeDrop
            {% endblock %}
        </title>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="icon" type="image/png" href="/favicon/favicon-96x96.png" sizes="96x96" />
        <link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg" />
        <link rel="shortcut icon" href="/favicon/favicon.ico" />
        <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
        <meta name="apple-mobile-web-app-title" content="Life Drop" />
        <link rel="manifest" href="/favicon/site.webmanifest" />

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    {# data-auth is a cheap, reliable flag you can target with CSS even if markup gets cached weirdly #}
    <body data-auth="{{ app.user ? '1' : '0' }}" class="min-h-screen text-slate-100 antialiased bg-slate-950">
        <div class="min-h-screen">
            {# Allow dropdowns to escape vertically while still preventing horizontal glow overflow #}
            <div class="relative overflow-x-hidden overflow-y-visible min-h-screen bg-slate-950">
                {# Background glow #}
                <div class="pointer-events-none absolute inset-0">
                    <div class="absolute -top-24 left-1/2 h-72 w-72 -translate-x-1/2 rounded-full bg-rose-600/25 blur-3xl"></div>
                    <div class="absolute top-40 right-[-120px] h-96 w-96 rounded-full bg-sky-500/20 blur-3xl"></div>
                </div>

                <div class="relative z-10 flex min-h-screen flex-col">
                    {# Header #}
                    <header class="relative z-50 border-b border-white/10 bg-slate-950/35 backdrop-blur">
                        <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
                            <a href="{{ path('app_home') }}" class="group flex items-center gap-3">
                                <div class="grid h-10 w-10 place-items-center rounded-2xl bg-white/10 ring-1 ring-white/15 overflow-hidden">
                                    <img src="{{ asset('images/logo.png') }}" alt="LifeDrop logo" class="h-7">
                                </div>
                                <div class="leading-tight">
                                    <div class="text-base font-semibold tracking-tight">
                                        LifeDrop
                                    </div>
                                    <div class="text-xs text-slate-300/80">
                                        Helping donors reach people in need
                                    </div>
                                </div>
                            </a>

                            {# Desktop nav #}
                            <nav class="hidden items-center gap-2 md:flex">
                                {% set route = app.request.attributes.get('_route') %}

                                {% if route == 'app_home' %}
                                    <a href="#how" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        How it works
                                    </a>
                                    <a href="#features" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        Features
                                    </a>
                                    <a href="#safety" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        Safety
                                    </a>
                                    <span class="mx-1 h-5 w-px bg-white/10"></span>
                                {% endif %}

                                {% if app.user %}
                                    <div class="relative group">
                                        <a href="{{ path('app_blood_request_index') }}" class="inline-flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold transition
                                            {{ route starts with 'app_blood_request_' ? 'bg-white/10 text-white ring-1 ring-white/15' : 'text-slate-200/90 hover:bg-white/5' }}">
                                            <i class="fa-solid fa-droplet" aria-hidden="true"></i>
                                            Requests
                                            <i class="fa-solid fa-chevron-down h-4 w-4 opacity-70 transition group-hover:opacity-100 group-focus-within:opacity-100" aria-hidden="true"></i>
                                        </a>

                                        <div class="invisible group-hover:visible group-focus-within:visible absolute left-0 right-0 top-full h-3"></div>

                                        <div class="invisible opacity-0 translate-y-1
                                                group-hover:visible group-hover:opacity-100 group-hover:translate-y-0
                                                group-focus-within:visible group-focus-within:opacity-100 group-focus-within:translate-y-0
                                                absolute left-0 top-full z-80 mt-2 w-72 rounded-2xl bg-slate-950/90 backdrop-blur
                                            ring-1 ring-white/12 shadow-xl shadow-black/30 transition duration-150">
                                            <div class="p-2">
                                                <a href="{{ path('app_blood_request_index') }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                    <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-white/7 ring-1 ring-white/10">
                                                        <i class="fa-solid fa-compass" aria-hidden="true"></i>
                                                    </div>
                                                    <div>
                                                        <div class="text-sm font-semibold text-slate-100">
                                                            Browse all requests
                                                        </div>
                                                        <div class="text-xs text-slate-400">
                                                            See active requests
                                                        </div>
                                                    </div>
                                                </a>

                                                <a href="{{ path('app_blood_request_mine') }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                    <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-white/7 ring-1 ring-white/10">
                                                        <i class="fa-solid fa-thumbtack" aria-hidden="true"></i>
                                                    </div>
                                                    <div>
                                                        <div class="text-sm font-semibold text-slate-100">
                                                            My requests
                                                        </div>
                                                        <div class="text-xs text-slate-400">
                                                            Track & edit your posts
                                                        </div>
                                                    </div>
                                                </a>

                                                <a href="{{ path('app_donation_offer_mine') }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                    <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-white/7 ring-1 ring-white/10">
                                                        <i class="fa-solid fa-handshake" aria-hidden="true"></i>
                                                    </div>
                                                    <div>
                                                        <div class="text-sm font-semibold text-slate-100">
                                                            My offers
                                                        </div>
                                                        <div class="text-xs text-slate-400">
                                                            Track your help offers
                                                        </div>
                                                    </div>
                                                </a>

                                                <div class="my-2 h-px bg-white/10"></div>

                                                <a href="{{ path('app_blood_request_new') }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                    <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-rose-600/20 ring-1 ring-rose-400/20 text-rose-100">
                                                        <i class="fa-solid fa-plus" aria-hidden="true"></i>
                                                    </div>
                                                    <div>
                                                        <div class="text-sm font-semibold text-slate-100">
                                                            Create a request
                                                        </div>
                                                        <div class="text-xs text-slate-400">
                                                            Post a new urgent need
                                                        </div>
                                                    </div>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                {% else %}
                                    <a href="{{ path('app_blood_request_index') }}" class="inline-flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold
                                        {{ route starts with 'app_blood_request_' ? 'bg-white/10 text-white ring-1 ring-white/15' : 'text-slate-200/90 hover:bg-white/5' }}">
                                        <i class="fa-solid fa-droplet" aria-hidden="true"></i>
                                        Requests
                                    </a>
                                {% endif %}

                                <a href="{{ app.user ? path('app_blood_request_new') : path('app_login') }}" class="ml-1 inline-flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-semibold text-white
                                        bg-rose-600 hover:bg-rose-500 shadow-lg shadow-rose-600/25
                                    focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-300/60">
                                    <i class="fa-solid fa-plus" aria-hidden="true"></i>
                                    Create request
                                </a>

                                <span class="mx-2 h-5 w-px bg-white/10"></span>

                                <div class="flex items-center gap-2">
                                    {% if app.user %}
                                        {% set profile = app.user.userProfile ?? null %}
                                        {% set isProfileComplete = profile and (profile.fullName|default('')|trim) and (profile.city|default('')|trim) and (profile.phone|default('')|trim) %}
                                        {% set fullName = profile and (profile.fullName|default('')|trim) ? profile.fullName|trim : 'Account' %}
                                        {% set initials = (fullName|trim|split(' ')|map(v => v|first)|join('')|upper)|slice(0,2) %}

                                        <div class="relative group">
                                            <a href="{{ path('app_profile') }}" class="inline-flex items-center gap-3 rounded-2xl px-3 py-2 ring-1 ring-white/10 bg-white/5 hover:bg-white/7 transition">
                                                <div class="grid h-9 w-9 place-items-center rounded-xl bg-white/10 ring-1 ring-white/12 text-sm font-bold text-slate-100">
                                                    {{ initials }}
                                                </div>

                                                <div class="hidden lg:block leading-tight">
                                                    <div class="flex items-center gap-2">
                                                        <div class="text-sm font-semibold text-slate-100 max-w-40 truncate">
                                                            {{ fullName }}
                                                        </div>
                                                        {% if not isProfileComplete %}
                                                            <span class="rounded-full bg-amber-500/15 px-2 py-0.5 text-[11px] font-semibold text-amber-200 ring-1 ring-amber-400/20">
                                                                Complete
                                                            </span>
                                                        {% endif %}
                                                    </div>
                                                    <div class="text-xs text-slate-400">
                                                        Account
                                                    </div>
                                                </div>

                                                <i class="fa-solid fa-chevron-down h-4 w-4 text-slate-300/70 transition group-hover:text-slate-200 group-focus-within:text-slate-200" aria-hidden="true"></i>
                                            </a>

                                            <div class="invisible group-hover:visible group-focus-within:visible absolute left-0 right-0 top-full h-3"></div>

                                            <div class="invisible opacity-0 translate-y-1
                                                    group-hover:visible group-hover:opacity-100 group-hover:translate-y-0
                                                    group-focus-within:visible group-focus-within:opacity-100 group-focus-within:translate-y-0
                                                    absolute right-0 top-full z-80 mt-2 w-72 rounded-2xl bg-slate-950/90 backdrop-blur
                                                ring-1 ring-white/12 shadow-xl shadow-black/30 transition duration-150">
                                                <div class="p-2">
                                                    <a href="{{ path('app_profile') }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                        <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-white/7 ring-1 ring-white/10">
                                                            <i class="fa-solid fa-user" aria-hidden="true"></i>
                                                        </div>
                                                        <div>
                                                            <div class="text-sm font-semibold text-slate-100">
                                                                Profile
                                                            </div>
                                                            <div class="text-xs text-slate-400">
                                                                View your details
                                                            </div>
                                                        </div>
                                                    </a>

                                                    {% if not isProfileComplete %}
                                                        <div class="mt-2 rounded-xl bg-amber-500/10 ring-1 ring-amber-400/20 px-3 py-2 text-xs text-amber-100">
                                                            Finish your profile to create requests faster.
                                                        </div>
                                                    {% endif %}

                                                    <div class="my-2 h-px bg-white/10"></div>

                                                    <a href="{{ logout_path() }}" class="flex items-start gap-3 rounded-xl px-3 py-2 hover:bg-white/5">
                                                        <div class="mt-0.5 grid h-9 w-9 place-items-center rounded-xl bg-rose-600/15 ring-1 ring-rose-400/20 text-rose-100">
                                                            <i class="fa-solid fa-right-from-bracket" aria-hidden="true"></i>
                                                        </div>
                                                        <div>
                                                            <div class="text-sm font-semibold text-slate-100">
                                                                Logout
                                                            </div>
                                                            <div class="text-xs text-slate-400">
                                                                Sign out
                                                            </div>
                                                        </div>
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    {% else %}
                                        <a href="{{ path('app_login') }}" class="rounded-xl px-4 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10">
                                            Log in
                                        </a>
                                        <a href="{{ path('app_register') }}" class="rounded-xl bg-rose-600 px-4 py-2 text-sm font-semibold text-white hover:bg-rose-500 shadow-lg shadow-rose-600/25">
                                            Create account
                                        </a>
                                    {% endif %}
                                </div>
                            </nav>

                            {# Mobile nav (cache-proof): render both blocks, then show via body[data-auth] #}
                            <div class="flex items-center gap-2 md:hidden">
                                <a href="{{ path('app_blood_request_index') }}" class="rounded-xl px-3 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10">
                                    Requests
                                </a>

                                {# Logged-in mobile buttons #}
                                <div class="hidden [body[data-auth='1']_&]:flex items-center gap-2">
                                    <a href="{{ path('app_donation_offer_mine') }}" class="rounded-xl px-3 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10">
                                        My offers
                                    </a>

                                    <a href="{{ path('app_blood_request_new') }}" class="rounded-xl bg-rose-600 px-3 py-2 text-sm font-semibold text-white hover:bg-rose-500 inline-flex items-center gap-2">
                                        <i class="fa-solid fa-plus" aria-hidden="true"></i>
                                        New
                                    </a>

                                    <a href="{{ logout_path() }}" class="rounded-xl px-3 py-2 text-sm font-semibold text-slate-100 ring-1 ring-white/10 hover:bg-white/5 inline-flex items-center gap-2">
                                        <i class="fa-solid fa-right-from-bracket" aria-hidden="true"></i>
                                        Logout
                                    </a>
                                </div>

                                {# Logged-out mobile buttons #}
                                <div class="hidden [body[data-auth='0']_&]:flex items-center gap-2">
                                    <a href="{{ path('app_login') }}" class="rounded-xl px-3 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10 inline-flex items-center gap-2">
                                        <i class="fa-solid fa-right-to-bracket" aria-hidden="true"></i>
                                        Log in
                                    </a>
                                    <a href="{{ path('app_register') }}" class="rounded-xl bg-rose-600 px-3 py-2 text-sm font-semibold text-white hover:bg-rose-500 inline-flex items-center gap-2">
                                        <i class="fa-solid fa-user-plus" aria-hidden="true"></i>
                                        Sign up
                                    </a>
                                </div>
                            </div>
                        </div>
                    </header>

                    <main class="flex-1">
                        {% block body %}{% endblock %}
                    </main>

                    {# Footer #}
                    <footer class="border-t border-white/10 bg-slate-950/35 backdrop-blur">
                        <div class="mx-auto max-w-6xl px-6 py-8">
                            <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
                                <div class="text-xs text-slate-400">
                                    ©
                                    {{ "now"|date("Y") }}
                                    LifeDrop • Built for the community
                                </div>

                                <div class="flex flex-wrap items-center gap-3 text-xs">
                                    <a href="{{ path('app_home') }}#how" class="text-slate-400 hover:text-slate-200">
                                        How it works
                                    </a>
                                    <span class="text-slate-700">
                                        •
                                    </span>
                                    <a href="{{ path('app_home') }}#features" class="text-slate-400 hover:text-slate-200">
                                        Features
                                    </a>
                                    <span class="text-slate-700">
                                        •
                                    </span>
                                    <a href="{{ path('app_home') }}#safety" class="text-slate-400 hover:text-slate-200">
                                        Safety
                                    </a>
                                </div>
                            </div>

                            <div class="mt-3 text-[11px] leading-relaxed text-slate-500">
                                LifeDrop is a community connector. For medical advice and donation rules, follow your hospital’s guidance.
                            </div>
                        </div>
                    </footer>
                </div>
            </div>
        </div>
    </body>
</html>
