<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                LifeDrop
            {% endblock %}
        </title>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="icon" type="image/png" href="/favicon/favicon-96x96.png" sizes="96x96" />
        <link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg" />
        <link rel="shortcut icon" href="/favicon/favicon.ico" />
        <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
        <meta name="apple-mobile-web-app-title" content="Life Drop" />
        <link rel="manifest" href="/favicon/site.webmanifest" />

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body class="min-h-screen text-slate-100 antialiased bg-slate-950">
        <div class="min-h-screen">
            <div class="relative overflow-hidden min-h-screen bg-slate-950">
                {# Background glow #}
                <div class="pointer-events-none absolute inset-0">
                    <div class="absolute -top-24 left-1/2 h-72 w-72 -translate-x-1/2 rounded-full bg-rose-600/25 blur-3xl"></div>
                    <div class="absolute top-40 right-[-120px] h-96 w-96 rounded-full bg-sky-500/20 blur-3xl"></div>
                </div>

                <div class="relative z-10 flex min-h-screen flex-col">
                    {# Header: glassy (so it matches your cards) #}
                    <header class="border-b border-white/10 bg-slate-950/35 backdrop-blur">
                        <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
                            <a href="{{ path('app_home') }}" class="group flex items-center gap-3">
                                <div class="grid h-10 w-10 place-items-center rounded-2xl bg-white/10 ring-1 ring-white/15 overflow-hidden">
                                    <img src="{{ asset('images/logo.png') }}" alt="LifeDrop logo" class="h-7">
                                </div>
                                <div class="leading-tight">
                                    <div class="text-base font-semibold tracking-tight">
                                        LifeDrop
                                    </div>
                                    <div class="text-xs text-slate-300/80">
                                        Helping donors reach people in need
                                    </div>
                                </div>
                            </a>

                            <nav class="hidden items-center gap-2 md:flex">
                                {% if app.request.attributes.get('_route') == 'app_home' %}
                                    <a href="#how" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        How it works
                                    </a>
                                    <a href="#features" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        Features
                                    </a>
                                    <a href="#safety" class="rounded-xl px-3 py-2 text-sm text-slate-200/90 hover:bg-white/5">
                                        Safety
                                    </a>
                                {% endif %}

                                <div class="ml-2 flex items-center gap-2">
                                    {% if app.user %}
                                        <span class="hidden lg:inline text-sm text-slate-300/80">
                                            Hi,
                                            <span class="text-slate-100 font-semibold">
                                                {{ app.user.userIdentifier }}
                                            </span>
                                        </span>
                                        <a href="{{ logout_path() }}" class="rounded-xl px-4 py-2 text-sm font-semibold text-slate-100 ring-1 ring-white/10 hover:bg-white/5">
                                            Logout
                                        </a>
                                    {% else %}
                                        <a href="{{ path('app_login') }}" class="rounded-xl px-4 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10">
                                            Log in
                                        </a>
                                        <a href="{{ path('app_register') }}" class="rounded-xl bg-rose-600 px-4 py-2 text-sm font-semibold text-white hover:bg-rose-500 shadow-lg shadow-rose-600/25">
                                            Create account
                                        </a>
                                    {% endif %}
                                </div>
                            </nav>

                            <div class="flex items-center gap-2 md:hidden">
                                {% if app.user %}
                                    <a href="{{ logout_path() }}" class="rounded-xl px-3 py-2 text-sm font-semibold text-slate-100 ring-1 ring-white/10 hover:bg-white/5">
                                        Logout
                                    </a>
                                {% else %}
                                    <a href="{{ path('app_login') }}" class="rounded-xl px-3 py-2 text-sm font-medium text-slate-100 hover:bg-white/5 ring-1 ring-white/10">
                                        Log in
                                    </a>
                                    <a href="{{ path('app_register') }}" class="rounded-xl bg-rose-600 px-3 py-2 text-sm font-semibold text-white hover:bg-rose-500">
                                        Sign up
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    </header>

                    <main class="flex-1">
                        {% block body %}{% endblock %}
                    </main>

                    {# Footer: same glass surface as header #}
                    <footer class="border-t border-white/10 bg-slate-950/35 backdrop-blur">
                        <div class="mx-auto max-w-6xl px-6 py-8">
                            <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
                                <div class="text-xs text-slate-400">
                                    ©
                                    {{ "now"|date("Y") }}
                                    LifeDrop • Built for the community
                                </div>

                                <div class="flex flex-wrap items-center gap-3 text-xs">
                                    <a href="{{ path('app_home') }}#how" class="text-slate-400 hover:text-slate-200">
                                        How it works
                                    </a>
                                    <span class="text-slate-700">
                                        •
                                    </span>
                                    <a href="{{ path('app_home') }}#features" class="text-slate-400 hover:text-slate-200">
                                        Features
                                    </a>
                                    <span class="text-slate-700">
                                        •
                                    </span>
                                    <a href="{{ path('app_home') }}#safety" class="text-slate-400 hover:text-slate-200">
                                        Safety
                                    </a>
                                </div>
                            </div>

                            <div class="mt-3 text-[11px] leading-relaxed text-slate-500">
                                LifeDrop is a community connector. For medical advice and donation rules, follow your hospital’s guidance.
                            </div>
                        </div>
                    </footer>
                </div>
            </div>
        </div>
    </body>
</html>
